---
title: "Rapport d'études  Rmarkdown"
author: "MARGERAND Timéo et DELIN Mattéo"
output: html_document
---

```{r setup, include=T}
knitr::opts_chunk$set(echo = F)
```
```{r, eval=FALSE}
install.packages(c("rmarkdown", "knitr", "ggplot2"))
setwd("C:/Users/timeo/Documents/cours/année 2/r projet (r markdown et r shiny)")
fichiers = list.files(pattern = "\\.csv$")
df_dpe = read.csv(fichiers[1])
for(f in fichiers[-1]){
  df_dpe = rbind(df_dpe)}
}



# Tableau freq en % des classes energies 
tab_classes <- table(df_dpe$etiquette_dpe)
prop.table(tab_classes) * 100

barplot(tab_classes,
        main = "Répartition des classes énergétiques en Auvergne-Rhône-Alpes",
        xlab = "Classe énergétique",
        ylab = "Nombre de logements")
#Tableaux croisé qualité_iso_
tab_iso_note <- table(df_dpe$etiquette_dpe,df_dpe$qualite_isolation_enveloppe)*100
tab_iso_note=prop.table(tab_iso_note)*100
#Voir pour analyse bivariée

```

